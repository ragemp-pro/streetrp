import React from 'react';
import moment from 'moment-timezone';

type Props = {
	expires?: string;
};

export default function PlayerInfoVip({ expires }: Props) {
	return <div className="player-info_vip"></div>;
}
